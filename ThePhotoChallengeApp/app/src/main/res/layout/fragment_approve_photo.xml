<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    tools:context=".views.fragments.ApprovePhotoFragment">

    <ImageView
        android:id="@+id/new_photo_viewer"
        android:layout_marginTop="50dp"
        android:layout_marginBottom="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/background"/>

    <ImageView
        android:id="@+id/take_location_button"
        android:layout_margin="30dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/flat_location"
        android:layout_below="@+id/new_photo_viewer"
        android:layout_centerHorizontal="true"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_action_tick"
        android:layout_toStartOf="@+id/take_location_button"
        android:layout_below="@id/take_location_button"
        android:layout_alignParentBottom="true"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_action_cancel"
        android:layout_below="@+id/take_location_button"
        android:layout_toEndOf="@+id/take_location_button"
        android:layout_alignParentBottom="true"/>





</RelativeLayout>
